# Weather One

## Features
- Single-page React + Tailwind UI
- Search by place name with live suggestions (Open-Meteo geocoding)
- Search by latitude/longitude
- Use browser geolocation for quick lookup
- Current weather snapshot (temp, feels like, precipitation, wind)
- No API key required (Open-Meteo)

## Run locally
```bash
npm install
npm run dev
```
